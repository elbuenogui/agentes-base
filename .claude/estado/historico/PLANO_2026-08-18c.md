## Objetivo
Validar se a API da OpenAI produz transcrição de qualidade suficiente para entrevistas em
português brasileiro, construindo primeiro o motor mínimo (áudio → API → transcrição) antes de
qualquer camada de persistência, IA adicional ou multi-dispositivo.

## Escopo
- Dentro:
  - estrutura inicial do projeto em `transcritor/` (`backend/`, `frontend/`, `.env`,
    `requirements.txt`, `README.md`);
  - preenchimento dos placeholders `<...>` nos arquivos do kit (`CLAUDE.md` e demais) com o
    contexto deste projeto;
  - proteção de segredos e artefatos locais (`.env`, `.venv/`, `__pycache__/`) no controle de
    versão;
  - backend que recebe áudio, guarda a API key exclusivamente no servidor e chama a API da
    OpenAI;
  - upload de arquivo de áudio existente (`.m4a`, `.wav`, `.mp3`);
  - gravação de áudio pelo navegador, com seleção do dispositivo de entrada;
  - seleção do modelo de transcrição (GPT-4o Transcribe, GPT-4o mini Transcribe, GPT-4o
    Transcribe Diarize);
  - exibição da transcrição na tela;
  - benchmark comparativo entre os três modelos no mesmo áudio (qualidade e custo).
- Fora (explícito): login, banco de dados, histórico, RAG, embeddings, busca semântica, análise
  qualitativa, resumo automático, múltiplos agentes, integração com smartwatch, transcrição em
  tempo real, app Android nativo, sincronização entre dispositivos. `README.md` da raiz do
  repositório (documentação do kit agentes-base) não é alterado — o projeto tem seu próprio
  README em `transcritor/README.md`.

## Etapas
1. [Estrutura inicial do projeto e customização do kit] — Status: **concluída** (2026-08-16).
2. [Proteger transcritor/.env no controle de versão] — Status: **concluída** (2026-08-16).
3. [Backend — endpoint de transcrição] — Status: **concluída** (2026-08-16), validada de ponta
   a ponta com áudio e chave reais.
4. [Seleção de modelo (+ housekeeping do .gitignore)] — Status: **concluída** (2026-08-16), três
   modelos testados com áudio real.
5. [Frontend — upload de arquivo] — Status: **concluída** (2026-08-16, ver PROGRESSO.md).
   Verificado no artefato real pelo PM: `transcritor/frontend/index.html` tem seletor de
   arquivo, seletor dos três modelos, chamada para `POST /transcrever`, tratamento de sucesso/
   erro/carregando, nenhuma chave de API no HTML/JS; `main.py` ganhou só o middleware de CORS
   (nada da lógica de transcrição mudou).
6. [Frontend — gravação pelo microfone] — Status: **concluída** (2026-08-18). Verificado no
   artefato real pelo PM: `transcritor/frontend/index.html` tem seletor de dispositivo de
   entrada (`enumerateDevices`, repopulado após a permissão), botão de gravação com indicador
   visual (ponto pulsando) e cronômetro mm:ss, player de prévia (`<audio controls>`) e botão
   dedicado "Transcrever gravação" que reaproveita o seletor de modelo e a área de resultado já
   existentes; erros de permissão negada, ausência de dispositivo e navegador sem suporte
   tratados na mesma área de erro; `transcritor/README.md` documenta o fluxo de gravação. Um bug
   de CSS (`[hidden]` sobrescrito por regra `display` mais específica) foi encontrado e corrigido
   pelo Executor durante a própria validação, com reexecução dos testes depois da correção.
7. [Benchmark entre modelos] — Status: **concluída** (2026-08-18). Verificado no artefato real
   pelo PM: `transcritor/BENCHMARK.md` traz os três modelos com tempo de resposta, custo
   estimado (com fonte/data do preço citada) e transcrição lado a lado; `transcritor/benchmark.py`
   reaproveita o endpoint existente, sem chamada à API fora do backend; `transcritor/README.md`
   documenta como rodar. Durante a execução, o modelo `gpt-4o-transcribe-diarize` expôs um bug
   real e pré-existente do backend (a API da OpenAI passou a exigir `chunking_strategy` para
   modelos de diarização, e `backend/main.py` não enviava esse parâmetro) — afetava também o uso
   normal do frontend (upload e gravação), não só o benchmark. Ver nota abaixo sobre a correção.

> Plano no máximo de 7 etapas (limite do papel PM). Todas as 7 etapas do plano estão concluídas —
> MVP validado de ponta a ponta (upload, gravação e os três modelos de transcrição). Itens novos
> de escopo a partir daqui vão para o Backlog e só sobem para Etapas com aprovação explícita (o
> que exigiria abrir um plano novo, já que este está no limite).

## Backlog (não aprovado)
- CORS do backend está aberto (`allow_origins=["*"]`), aceitável para uso 100% local — revisar
  se um dia o projeto for servido fora da máquina do usuário (risco reportado pelo Executor na
  Etapa 5);
- histórico, armazenamento por projeto/entrevista, exportação, metadados (Fase 1 do rascunho
  original);
- pipeline de limpeza → segmentação → classificação → resumo com modelos especializados (Fase
  2);
- banco de dados, busca semântica, embeddings, RAG, corpus de entrevistas (Fase 3);
- camada de agente que decide qual modelo/informação usar (Fase 4);
- suporte a celular e smartwatch (Fase 5);
- transcrição em tempo real.

## Exceção registrada (regra do papel PM)
- **2026-08-16** — decisão explícita do usuário: quando o Executor reportar, no PROGRESSO.md,
  um risco de segurança claro e de correção pequena/rápida, o PM pode gerar a
  `PROXIMA_TAREFA.md` de correção diretamente, sem perguntar antes — desde que: (a) risco
  objetivo e verificável no artefato; (b) correção pequena; (c) plano continue dentro do limite
  de 7 etapas. Fora desses critérios, volta a regra padrão de perguntar antes.
  - Aplicada na Etapa 2 (`.gitignore` para `transcritor/.env`).
  - Na Etapa 3 surgiu novo risco pequeno (`.venv/` fora do `.gitignore`); dobrado dentro da
    Etapa 4 por já estar no limite de 7 — resolvido.
  - Na Etapa 5, o CORS aberto foi para o Backlog (não é um risco imediato em uso local, critério
    "a" — objetivo e verificável como problema agora — não se aplica claramente).

## Nota de processo (lembrete para o PM)
- Ao gerar `PROXIMA_TAREFA.md` com passo de documentação, incluir `transcritor/README.md`
  explicitamente em "Arquivos envolvidos" (regra seguida a partir da Etapa 5).
- **2026-08-18** — na Etapa 7, o usuário pediu a correção do `chunking_strategy` diretamente ao
  Executor no chat de execução, fora do fluxo normal PM → `PROXIMA_TAREFA.md` → Executor (o
  Executor sinalizou isso explicitamente no PROGRESSO.md como desvio de processo). A correção em
  si foi pequena, objetiva e bem verificada (ver Etapa 7 acima e a coleta), mas fica registrado
  aqui que o `backend/main.py` foi alterado sem uma `PROXIMA_TAREFA.md` gerada pelo PM para isso
  — não é a exceção de segurança já registrada acima (esse bug era funcional, não de segurança).
