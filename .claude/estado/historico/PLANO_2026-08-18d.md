## Objetivo
Refinar a experiência de gravação do assistente para ficar mais parecida com apps de voz
modernos (ex.: modo de voz do ChatGPT): botão redondo, clique único para começar/parar,
transcrição automática, transcript editável que acumula várias gravações. Simplificar a escolha
de modelo, removendo o modelo de diarização da interface (o usuário não gostou da qualidade) e
trocando o seletor de 3 opções por um alternador simples entre os dois modelos restantes.

## Escopo
- Dentro:
  - botão redondo com ícone simples (a critério do Executor — ex.: um pássaro estilo
    "maritaca", em SVG/emoji, sem precisar de arquivo de imagem externo) substituindo toda a
    seção de gravação atual da Etapa 6 do plano anterior (seletor de dispositivo, botão
    iniciar/parar, player de prévia, botão dedicado "Transcrever gravação");
  - interação de clique único: primeiro clique inicia a gravação imediatamente; segundo clique
    para a gravação e envia automaticamente para transcrição, sem botão extra de enviar;
  - área de transcript dedicada à gravação rápida, editável a qualquer momento (o usuário pode
    alterar o texto à mão) e cumulativa: cada nova gravação transcrita é adicionada ao final do
    texto já existente, sem apagar o que já tinha;
  - remoção do modelo `gpt-4o-transcribe-diarize` da interface — comentado no código do
    frontend, não deletado (dá para reativar depois); backend não muda;
  - substituição do seletor de 3 modelos (`<select>`) por um botão simples de alternar entre os
    dois modelos restantes (`gpt-4o-transcribe` e `gpt-4o-mini-transcribe`), sem caixa de
    seleção; esse alternador vale tanto para a gravação rápida quanto para o upload de arquivo;
  - fluxo de upload de arquivo mantido como está hoje (botão "escolher arquivo", envio,
    resultado substituindo o anterior a cada novo envio) — só troca o seletor de modelo pelo
    alternador acima.
- Fora (explícito):
  - qualquer mudança em `backend/main.py`;
  - reativar ou usar o modelo diarize em qualquer fluxo da interface;
  - upload de arquivo também acumular transcript (upload continua substituindo; só a gravação
    rápida acumula);
  - seleção de dispositivo de microfone — a versão nova assume o microfone padrão do navegador,
    sem escolher dispositivo (assunção do PM, aprovada pelo usuário em 2026-08-18).

## Etapas
1. [Alternador de modelo (sem diarize)] — critério de pronto: `<select>` de 3 modelos removido;
   botão simples alterna entre `gpt-4o-transcribe` e `gpt-4o-mini-transcribe`, indicando
   visualmente qual está ativo; upload de arquivo usa o modelo escolhido no alternador; opção
   diarize comentada no código (não deletada); testado com áudio real nos dois modelos
   restantes, sem regressão no fluxo de upload.
2. [Botão redondo de gravação rápida] — critério de pronto: botão redondo com ícone substitui a
   seção de gravação da Etapa 6 do plano anterior (device select, player de prévia, botão
   dedicado removidos); primeiro clique grava, segundo clique para e envia automaticamente;
   transcript aparece em área editável; nova gravação soma texto ao final do transcript
   existente sem apagar; usa o modelo ativo no alternador da Etapa 1; testado com fala real,
   incluindo edição manual do texto e pelo menos duas gravações seguidas confirmando a soma.

> Plano no máximo de 7 etapas (limite do papel PM). Plano novo, aberto em 2026-08-18 após o
> fechamento do plano do MVP de transcrição (7/7 etapas concluídas — ver
> `.claude/estado/historico/PLANO_2026-08-18c.md`). Qualquer item novo de escopo a partir daqui
> vai para o Backlog e só sobe para Etapas com aprovação explícita.

## Backlog (não aprovado)
- CORS do backend está aberto (`allow_origins=["*"]`), aceitável para uso 100% local — revisar
  se um dia o projeto for servido fora da máquina do usuário;
- seleção de dispositivo de microfone (removida da interface neste plano) — poderia voltar como
  opção avançada, se um dia for necessário escolher entre vários microfones;
- histórico, armazenamento por projeto/entrevista, exportação, metadados;
- pipeline de limpeza → segmentação → classificação → resumo com modelos especializados;
- banco de dados, busca semântica, embeddings, RAG, corpus de entrevistas;
- camada de agente que decide qual modelo/informação usar;
- suporte a celular e smartwatch;
- transcrição em tempo real;
- explorar plataforma de chat self-hosted tipo Claude com agentes/skills (Open WebUI, LibreChat,
  AnythingLLM, Dify, LobeHub) como possível motor para uma fase futura — discutido em
  2026-08-18, decisão de continuar em outro chat, nenhuma ação tomada neste projeto ainda.

## Nota de processo (lembrete para o PM)
- Ao gerar `PROXIMA_TAREFA.md` com passo de documentação, incluir `transcritor/README.md`
  explicitamente em "Arquivos envolvidos".
